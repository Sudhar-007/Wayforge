# Production build config for Vite. VITE_API_BASE_URL is baked into the bundle at
# build time (read in src/App.tsx and src/store/roadmapStore.ts). Not a secret —
# the backend URL is already public.
VITE_API_BASE_URL=https://pathfinder-api.jollyflower-7f6656d7.southeastasia.azurecontainerapps.io
